<Page
    x:Class="Carputer.UWP.Views.ShellView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Carputer.UWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:cm="using:Caliburn.Micro"
    mc:Ignorable="d">

    <Page.Resources>
        <SolidColorBrush x:Key="AccentBrush" Color="#dd333333" />
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Image Source="/Images/milky_way.jpg" Opacity="0.5" Stretch="UniformToFill"></Image>
        <SplitView x:Name="NavigationView" DisplayMode="Overlay" OpenPaneLength="150">
            <SplitView.Pane>
                <StackPanel Background="{ThemeResource AccentBrush}">
                    <Button x:Name="GoBack" Content="Back" Margin="24" />
                </StackPanel>
            </SplitView.Pane>
            <SplitView.Content>
                <Grid>
                    <Frame cm:Message.Attach="[Event Loaded] = [SetupNavigationService($source)]" DataContext="{x:Null}" />
                    <Button Click="OpenNavigationView" Content="&#xE700;" FontFamily="{ThemeResource SymbolThemeFontFamily}" VerticalAlignment="Top" Margin="24" />
                </Grid>
            </SplitView.Content>
        </SplitView>
    </Grid>
</Page>
